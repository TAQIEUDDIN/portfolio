# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
language: "en-US"

reviews:
  auto_review:
    enabled: true
    # The ".*" is a regular expression that matches every branch name
    base_branches:
      - ".*"